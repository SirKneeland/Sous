// PatchDecision.Decision, PatchDecision, and NextLLMContext are declared Codable
// directly in LLMRequest.swift so the compiler can synthesise encode/decode automatically.
// This file is intentionally empty — it exists to document the persistence intent.
